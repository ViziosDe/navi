% john

# Crack Kerberoast hashes using a wordlist
john --wordlist=<wordlist-path> <hashes-path>

# Crack NTLM hashes using incremental mode
john --format=NT --incremental <hash-file>

# Show the cracked passwords for a given hash file
john --show <hashes-path>
